import { BaxdirDirective } from './baxdir.directive';

describe('BaxdirDirective', () => {
  it('should create an instance', () => {
    const directive = new BaxdirDirective();
    expect(directive).toBeTruthy();
  });
});
